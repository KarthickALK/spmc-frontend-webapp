{
  "buildCommand": "if [ ! -d flutter ]; then git clone https://github.com/flutter/flutter.git -b stable --depth 1; fi && export PATH=\"$PATH:`pwd`/flutter/bin\" && flutter/bin/flutter config --no-analytics && flutter/bin/flutter pub get && flutter/bin/flutter build web --release",
  "outputDirectory": "build/web",
  "installCommand": "echo 'skipping npm install'",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}